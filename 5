package main

import (
	"fmt"
	"strings"
)

func addTwoAndTwo() int {
	twoStr := strings.Repeat("I", 2)
	fourStr := twoStr + twoStr

	sum := 0

	for range fourStr {
		sum++
	}

	return sum
}

func main() {
	result := addTwoAndTwo()
	fmt.Printf("2 + 2 = %d\n", result)
}
